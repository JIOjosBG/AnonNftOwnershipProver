## How to run 

To generate a proof:
`RISC0_DEV_MODE=1 ./target/release/host prover`

To verify a proof:
`RISC0_DEV_MODE=1 ./target/release/host verifier`



## How to run tests
`RISC0_DEV_MODE=1 cargo test --package host`

